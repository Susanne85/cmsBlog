{{#each blogs as | blog|}}
{{> blog-details}}
{{/each}}

{{#if loggedIn}}
<section>
   <form class="form newPost-form">
      <button class="btn btn-post" type="submit">New Post</button>
   </form>
   <script src="./js/dashboard.js"></script>
</section>
{{/if}}